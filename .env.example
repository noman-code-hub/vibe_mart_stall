# Local development + Vercel serverless.
#
# // WORDPRESS production: key lives in WP Settings → Vibe Mart, or
# //   define( 'VIBE_MART_REMOVE_BG_API_KEY', '...' ); in wp-config.php
#
# Get a key at https://www.remove.bg/api
# For Vercel: Project Settings → Environment Variables → REMOVE_BG_API_KEY
REMOVE_BG_API_KEY=your_remove_bg_api_key_here

# Upload limits for the remove-bg handler (10 MB default)
MAX_UPLOAD_BYTES=10485760

# Optional: proxy API from Vite to a real WordPress site with the plugin.
# Leave unset to use the local file-backed mock (.local-data/).
# // WORDPRESS local testing — not used on Vercel (Vercel uses /api/vm/v1).
# Example: WP_PROXY_TARGET=http://localhost/vibe-mart
# WP_PROXY_TARGET=