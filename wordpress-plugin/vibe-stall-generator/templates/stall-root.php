<?php
/**
 * Mount container for the React stall generator.
 *
 * Copy this file into your theme to wrap the container in extra markup; the id
 * must stay "stall-root" because the bundle mounts into it.
 *
 * @package VibeStallGenerator
 */

declare( strict_types = 1 );

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

?>
<div id="stall-root"></div>
